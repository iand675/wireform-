\* Replica / log-subscriber backends: reads may be served one token behind
\* (PostgreSQL read replica, Kafka consumer applying the log), floors stay
\* prefix-complete at the served snapshot. Safety must hold unchanged; the
\* NoFalseDegrade utility invariant is deliberately NOT asserted here
\* (genuine lag can exhaust K; that is what the Lattice-Snapshot-Min
\* request hint exists for). Must PASS.
CONSTANTS
  MaxVer = 4
  MaxRevalidate = 2
  Detection = "intervals"
  IndexAtCommit = TRUE
  LaggedReads = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK AcceptedImpliesCut
