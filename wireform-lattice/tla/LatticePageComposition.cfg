\* Conforming composed forms (spec 6.5 one-shot page, 12 page-subscription
\* bursts): the origin validates token stability around the multi-slice
\* execution and discards moved compositions. Must PASS.
CONSTANTS
  MaxVer = 3
  ValidateSnapshot = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK SinglePageSnapshot
