{- | Lattice: a cache-native graph query protocol.

Umbrella entry module re-exporting the codec surface (populated in the
final integration pass).
-}
module Lattice (
  module Lattice.Types,
) where

import Lattice.Types
