"The Star Wars corpus schema: characters, starships, and their reviews."
schema starwars.example.com

"API-stability marker surfaced to generated clients and the explorer."
directive @stability(level: Text = "stable") on ENTITY | FIELD | ROOT | MUTATION

newtype DroidId = Text

"Which film a thing appears in."
enum Episode closed = NewHope | Empire | Jedi

newtype HumanId = Text

newtype ReviewId = Text

newtype StarshipId = Text

"Anything in the saga with a name and friends: a Human or a Droid."
interface Character {
  appearsIn: [Episode]
  "The character's common name." name: Text
  has many friends: Character by ownerId ordered by name asc page 10 max 50
}

"An astromech, protocol, or other droid."
entity Droid by id implements Character {
  visible to all by default
  appearsIn: [Episode]
  id: DroidId
  name: Text
  "What the droid was built to do." primaryFunction: Text?
  has many friends: Character by ownerId ordered by name asc page 10 max 50
  fetch by id: public
}

"A person in the saga."
entity Human by id implements Character {
  visible to all by default
  appearsIn: [Episode]
  "The character's homeworld, if known." homePlanet: Text?
  id: HumanId
  name: Text
  has many friends: Character by ownerId ordered by name asc page 10 max 50
  fetch by id: public
}

"A fan review of one episode."
entity Review by id {
  visible to all by default
  commentary: Text?
  createdAt: Timestamp
  episode: Episode
  id: ReviewId
  "A rating from 0 to 5." stars: W8
  fetch by id: public
}

entity Starship by id {
  visible to all by default
  id: StarshipId
  length: F64?
  name: Text
  fetch by id: public
}

fragment CharacterName on Character {name}

"The hero of an episode (R2-D2 by default, Luke for Empire)."
get hero(episode: Episode?) of (Human | Droid) public

"Reviews of an episode, newest first."
list reviews of Review by episode ordered by createdAt desc page 10 max 50 public

"Full-text-ish name search across characters and starships."
@stability(level: "experimental")
list search(text: Text) of (Human | Droid | Starship) ordered by name asc page 10 max 50 public

"Post a new review of an episode."
@stability
mutation createReview(episode: Episode, stars: W8, commentary: Text?) returns Review {
  allow public
  writes Review(new), reviews(episode)
  invalidates writes
  effect transactional
}
